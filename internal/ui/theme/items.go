package theme

// Item form strings.
const (
	// FieldRemoveHint explains that clearing a label drops the field, which is
	// how the item form deletes one.
	FieldRemoveHint = "empty removes this field"

	// ItemNotLoadedStatus is shown when edit is pressed before the selected
	// item's field values have arrived.
	ItemNotLoadedStatus = "item not loaded yet"
)
